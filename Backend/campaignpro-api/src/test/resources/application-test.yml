spring:
  datasource:
    url: jdbc:tc:postgresql:16:///campaignpro_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-only-secret-not-for-production-use-32bytes

